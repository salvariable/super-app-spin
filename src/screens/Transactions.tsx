import React from 'react'
import { Text, View } from 'react-native'

type Props = {}

export const Transactions = (props: Props) => {
    return (
        <View>
            <Text>
                Transactions screen
            </Text>
        </View>
    )
}